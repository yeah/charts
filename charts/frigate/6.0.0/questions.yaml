groups:
  - name: "Fine tuning"
    description: "Fine tuning params"
questions:
  - variable: shm-size
    label: Shared memory size
    description: "Shared memory size"
    group: "Fine tuning"
    schema:
      type: string
      required: false
      default: 64m
